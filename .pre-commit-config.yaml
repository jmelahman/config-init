repos:
  - repo: local
    hooks:
      - id: so-generate
        name: regenerate C translation (generated/)
        entry: sh -c 'so translate -o generated .'
        language: system
        files: '(\.go|go\.mod|go\.sum)$'
        pass_filenames: false
