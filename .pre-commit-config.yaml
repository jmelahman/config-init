repos:
  - repo: local
    hooks:
      - id: check
        name: build and test with both toolchains
        entry: make check
        language: system
        files: '(\.go|go\.mod|so\.mod|go\.sum|so\.sum)$'
        pass_filenames: false
